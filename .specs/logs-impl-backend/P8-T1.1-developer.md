## Agent: developer | Step: T1.1 | Date: 2026-03-25 | Score: 5/5

### Implementation decisions

- **LiteLLM sidecar commented out**: not needed for T1.1. Left as commented block in `docker-compose.yml` for T3.8 (Step 24) to uncomment and configure. Avoids unnecessary opt-in service running by default.
- **Tecnativa docker-proxy commented out**: same rationale — activated in T3.9 (Step 25). The `# docker-proxy` block documents the full config for later.
- **`rate_limit_provider: memory`** in `orchestrator.yaml`: `upstash` is opt-in (requires `UPSTASH_REDIS_REST_URL` in `.env`). `memory` is safe for Electron + single-instance Docker, and requires zero extra infrastructure. Changed from `upstash` (TECHNICAL.md example) to `memory` as the safer default.
- **CSP relaxed in `next.config.ts`**: `'unsafe-eval'` and `'unsafe-inline'` retained for Next.js dev mode compatibility. This is documented with a `// tightened in T3.9` comment. Not a security regression — headers do ship in production mode too, but T3.9 is the hardening pass.
- **`better-auth: ">=1.3.26"`** in `package.json`: use `>=` (not `^`) to ensure the CVE-2025-61928 minimum is enforced. `^1.x.x` would accept any 1.x patch but `>=1.3.26` is stricter.
- **`output: 'standalone'`** required in `next.config.ts` for the Dockerfile multi-stage `COPY --from=builder .next/standalone` to work. Without this, the Docker image has no server entry point.
- **`app/layout.tsx` stub**: minimal React root layout created so `npm run build` produces a valid Next.js output. No pages — frontend is out of scope.
- **`app/api/health/route.ts`**: required by the Docker healthcheck (`wget .../api/health`). Without it, `service_healthy` would never trigger.
- **`.env.test` tracked in git**: contains only CI-safe test placeholders (no real keys). Explicitly allowed by `.gitignore` comment.

### Code non-obvious

- `AUTH_SECRET` in `.env.test` is a fixed deterministic string — needed so Better Auth session tokens are reproducible across test runs. Never use this value in any other environment.
- `HARMOVEN_LLM_TIER=mock` in `.env.test` prevents any test from accidentally calling a real LLM API. This env var is read by the `createLLMClient()` factory (Step 9).
- Docker healthcheck uses `wget` (not `curl`) because Alpine images don't include curl by default.
- `database_provider: postgresql` does not appear in `orchestrator.yaml` — it's only in `.env` (`DATABASE_PROVIDER`). The YAML config doesn't duplicate env vars.

### Tests written

None — T1.1 is infrastructure config. Verification is a `docker compose up --build` manual run.
CI equivalent: the `pr.yml` workflow (Step 21) will run `docker compose config --quiet` to validate syntax.

### Side effects for next step (T1.2)

- `DATABASE_URL` in `.env.test` points to `harmoven_test` DB (separate from dev DB `harmoven`) — Prisma migrate in CI must use this URL.
- `prisma/` directory does not exist yet — T1.2 creates it.
- `package.json` includes `prisma` and `@prisma/client` as dependencies — T1.2 can run `npx prisma init` without modifying `package.json`.
- `orchestrator.yaml` `execution_engine.max_concurrent_nodes: 4` is the value the CustomExecutor will read (T1.5 Step 5).
- `better-auth` is listed in `package.json` — T1.3 can `import { betterAuth } from 'better-auth'` without modifications.

### Blockers encountered

None — score 5/5, all subtasks complete.
