# P8-T3.3-developer.md — Harmoven v1
# Role: Developer (Phase 8 — T3.3)
# Date: 2026-03-26
# Task: T3.3 — Marketplace backend

---

## 1. Task summary

Implement the marketplace backend: pack installation, security scanning, update
management, and admin skill management routes.

**Spec refs:** TECHNICAL.md §39.5, Amendment 67 (install/update/overrides),
Amendment 68 (Bayesian rating), AGENTS-07 install-pack pseudocode.

**Done when:**
- [x] `lib/marketplace/types.ts` — PackManifest, PackScanResult, computeBayesianScore()
- [x] `lib/marketplace/scan.ts` — scanPackContent() with 9 injection patterns + URL check
- [x] `lib/marketplace/install-pack.ts` — installPack(), checkPackUpdates(), applyLocalOverride(), uninstallPack()
- [x] `app/api/admin/skills/route.ts` — GET list + POST install (instance_admin only)
- [x] `app/api/admin/skills/[id]/route.ts` — PATCH enable/config + DELETE
- [x] `orchestrator.yaml` — marketplace section added
- [x] 21 unit tests, 317 total passing (18 suites), 0 regressions

---

## 2. Discovery — what already existed

| Feature | Status |
|---------|--------|
| `McpSkill` Prisma model | ✅ Already in schema |
| `InstalledPack` Prisma model with `@@unique([user_id, pack_id])` | ✅ Already in schema |
| `AuditLog` model | ✅ Already in schema |
| `admin:skills` permission in built-in roles (admin + instance_admin) | ✅ Already in permissions.ts |
| `marketplace:install` permission | ✅ Already in permissions.ts |
| `resolveCaller()` + auth pattern | ✅ Already implemented |
| `lib/marketplace/` directory | ❌ Did not exist |
| `app/api/admin/` directory | ❌ Did not exist |
| Any marketplace routes | ❌ Did not exist |
| orchestrator.yaml marketplace section | ❌ Did not exist |

---

## 3. Files created / modified

| File | Change |
|------|--------|
| `lib/marketplace/types.ts` | New — PackManifest, PackScanResult, computeBayesianScore() |
| `lib/marketplace/scan.ts` | New — scanPackContent() local security scanner |
| `lib/marketplace/install-pack.ts` | New — full install/update/override/uninstall flow |
| `app/api/admin/skills/route.ts` | New — GET + POST |
| `app/api/admin/skills/[id]/route.ts` | New — PATCH + DELETE |
| `orchestrator.yaml` | Modified — marketplace section appended |
| `tests/marketplace/install-pack.test.ts` | New — 21 tests |

---

## 4. Key implementation decisions

### Security (§39.5 install flow)

**SHA-256 hash verification** — timing-safe comparison using `crypto.timingSafeEqual`
on two fixed-length 64-byte hex Buffers. Both strings are always 64 hex chars so
no length branch leaks timing info.

```typescript
const actualBuf   = Buffer.from(createHash('sha256').update(content).digest('hex'))
const expectedBuf = Buffer.from(expectedHash.toLowerCase())
timingSafeEqual(actualBuf, expectedBuf)  // throws HASH_MISMATCH on mismatch
```

**Prompt injection scan** — 9 regex patterns covering OWASP LLM Top-10:
`ignore previous instructions`, `you are now DAN`, ChatML `<|im_start|>`,
Llama `[INST]…[/INST]`, `disregard all prior`, `new instructions`, `system prompt`,
`act as [role]`, `forget everything`. Fast-fail (returns on first match).

**External URL filter** — blocks `http(s)://` unless domain is `harmoven.com`,
`localhost`, or `127.0.0.1`. Pack content cannot exfiltrate data at LLM runtime.

**Failed scans → AuditLog** — written before throwing, so even attacker-supplied
packs that fail scan leave a trace.

**GPG verification** — explicitly deferred to T3.8 (supply chain monitor), noted
in comments and orchestrator.yaml (`verify_gpg_signature: false`).

### Admin routes: instance_admin only

MCP skills are instance-wide resources (not project-scoped). Granting `admin:skills`
to project-level admins would let them install instance-level capabilities without
instance owner consent. Decision: admin routes check `caller.instanceRole === 'instance_admin'`
directly (same fast-path as `resolvePermissions()` for instance_admin).

### Prisma nullable JSON

Prisma 7 rejects plain `null` for nullable JSON fields — requires `Prisma.DbNull`
sentinel. Pattern used in `[id]/route.ts`:

```typescript
import { Prisma } from '@prisma/client'
let scanReport: Prisma.InputJsonValue | typeof Prisma.DbNull = Prisma.DbNull
```

### Update policy (Am.67.3)

| Bump type | update_policy=auto | update_policy=notify | update_policy=manual |
|-----------|-------------------|---------------------|---------------------|
| major     | notify (always)   | notify              | manual              |
| minor     | auto-install      | notify              | manual              |
| patch     | auto-install      | notify              | manual              |

`semverBumpType()` compares `[major, minor, patch]` tuples from parsed versions.

### Local overrides (Am.67.4)

Stored as `LocalOverride[]` JSON array in `InstalledPack.local_overrides`.
Each entry: `{ field, original, override, created_at }`.
On upsert (re-install / update): `local_overrides` column is NOT touched —
preserves user customizations across version bumps.

The `applyLocalOverride()` function replaces existing override for same `field`
(idempotent), or appends new entry.

### Pack + version validation

- Pack ID: `/^[a-z0-9_]{1,64}$/` — lowercase only to avoid case-folding issues
  on case-insensitive filesystems; no spaces/hyphens to prevent path traversal curiosities.
- Version: `/^\d{1,4}\.\d{1,4}\.\d{1,4}$/` — strict major.minor.patch; no pre-release
  suffixes (`-rc.1`) to keep DB and comparison logic simple for V1.
- Skill UUID: `/^[0-9a-f]{8}-…/i` on PATCH/DELETE :id param.

### Bayesian score formula (Am.68)

```typescript
function computeBayesianScore(rawAverage, ratingCount, globalAverage = 3.5, confidence = 10) {
  return (ratingCount * rawAverage + confidence * globalAverage) / (ratingCount + confidence)
}
```

C=10, m=3.5. With 0 ratings → returns 3.5 (prior). With 1000 ratings at 4.8 → ≈4.8.

---

## 5. Tests

```
tests/marketplace/install-pack.test.ts — 21 tests
  scanPackContent
    ✓ passes clean content
    ✓ detects "ignore previous instructions" injection
    ✓ detects DAN injection attempt
    ✓ detects ChatML injection
    ✓ detects Llama instruction injection
    ✓ blocks external URL (https)
    ✓ allows harmoven.com URLs
    ✓ allows localhost URLs
    ✓ scans multiline content and catches injection on any line
  computeBayesianScore
    ✓ returns global average for zero ratings
    ✓ shrinks a high raw rating toward mean for few reviews
    ✓ approaches raw average with many reviews
    ✓ returns correct formula result for (4.0, 5)
    ✓ respects custom global average and confidence
  installPack — input validation
    ✓ throws MarketplaceError for invalid pack_id
    ✓ throws MarketplaceError for semver with pre-release suffix
    ✓ throws MarketplaceError for missing userId
  installPack — SHA-256 verification
    ✓ throws HASH_MISMATCH when hash does not match content
    ✓ proceeds past hash check and scans content when hash matches
    ✓ throws INJECTION_DETECTED when content has injection after hash passes
    ✓ idempotent install (upsert) updates existing pack without overwriting local_overrides
```

Not covered (acceptable for V1):
- `checkPackUpdates()` — would require mocking N registry fetches
- `applyLocalOverride()` — needs deeper DB mock setup
- Admin route handlers — Next.js routes not unit-testable without test server

---

## 6. Self-evaluation: 3.5 / 5

| Criterion | Score | Notes |
|-----------|-------|-------|
| Security | 4/5 | Timing-safe hash, scan, pack ID/UUID validation, AuditLog, scan_status gate. Missing: code-level SSRF check on registry URL (relies on config + network policy — documented). |
| Spec completeness | 3.5/5 | All 4 install-pack functions, admin routes, Bayesian score, update policy. Gap: no user-level install route (`marketplace:install`), `checkPackUpdates()` is O(N) sequential. |
| Test coverage | 3.5/5 | 21 focused unit tests, zero regression. Gap: no tests for `checkPackUpdates()`, `applyLocalOverride()`, or admin route handlers. |
| Code quality | 3.5/5 | Clean separation, Prisma nullable JSON handled correctly, TS clean. Minor: `timingSafeEqual` imported inside function body (cosmetic), `MarketplaceError.code` typed as `string` (not a union). |

**Global: 3.5 / 5**

---

## 7. Git

```
Branch:  feature/T3.3-marketplace
Commit:  cca772b  feat(T3.3): Marketplace backend — install-pack, admin skill routes, scan
Merged:  a9124f1  Merge feature/T3.3-marketplace: Marketplace backend → develop
Log:     3233078  docs(log): T3.3 developer log + progress update
```
